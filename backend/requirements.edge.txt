# Edge node dependencies — stripped training stack, ARM64-compatible
# Tested target: Raspberry Pi 5 (Cortex-A76, ARM64, no CUDA)

# ── Inference runtime ──────────────────────────────────────────────────────────
ncnn                    # CNN model inference (XNNPACK-accelerated on Cortex-A76)

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
slowapi>=0.1.9

# ── Vision ─────────────────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0   # headless = no GUI deps (saves ~150 MB on RPi)
Pillow>=10.0.0
numpy>=1.26.0

# ── YOLO inference (Ultralytics handles its own runtime) ───────────────────────
ultralytics>=8.3.0

# ── Stream resolution ──────────────────────────────────────────────────────────
yt-dlp>=2024.1.0

# ── Utilities ──────────────────────────────────────────────────────────────────
pyyaml>=6.0.0
tqdm>=4.66.0

# ── Removed from full requirements.txt (training-only) ─────────────────────────
# torch / torchvision / timm    — no direct dep; CNN inference runs on ncnn.
#                                 (NOTE: ultralytics above still pulls torch in
#                                  transitively. For a lean, torch-free edge image
#                                  drop ultralytics too — YOLO detect/anomaly then
#                                  become hub-only; CNN occupancy still works.)
# scikit-learn                  — evaluation metrics, not needed on edge
# matplotlib / seaborn          — visualisation, not needed on edge
# openpyxl                      — Excel export, not needed on edge
